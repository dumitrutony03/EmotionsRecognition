# Pas 1 Instalam dependintele:
Varianta 1: pip install deepface opencv-python pillow tf-keras

Varianta 2: 
pip install deepface
pip install opencv-python
pip install pillow
pip install tf-keras

# Pas 2 Activează virtual environment (dacă nu e deja activ)
# Windows:
venv\Scripts\activate

# Pas 3 Rulează aplicația
python .\main.py
